// Enum

// numeric enums
enum Month {
    JAN = 1, // default 0
    FEB,
    MAR,
    APR,
    MAY
}

// string enum
enum Months {
    JAN = "January",
    FEB = "February",
    MAR = "March",
    APR = "April",
    MAY = "May"
}